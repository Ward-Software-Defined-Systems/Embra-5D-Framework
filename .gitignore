# macOS
.DS_Store

# Working / internal documents — kept on disk locally, never committed.
# (build brief, technical review, and the Claude Code agent guide)
VISUALIZER_BUILD_BRIEF.md
5D_FRAMEWORK_REVIEW.md
CLAUDE.md

# Python (verification suite) — the venv is environment-specific and regenerable
.venv/
__pycache__/
*.pyc

# Node / build artifacts (the app/ has its own .gitignore too; these are repo-wide safety nets)
node_modules/
dist/
dist-ssr/
.wrangler/
.dev.vars*
*.local
*.log
